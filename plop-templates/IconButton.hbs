<!--  Generated from IconButton plop template -->

<template>
  <div class="btn hover:scale-110 transition" @click.stop="emit(EMIT.CLICK, this)">
    <FontIconComponent
        :icon-type="FontIconType.{{ upperCase class }}"
        :fontIconSize="props.fontIconSize"
        :clickable="false"
    ></FontIconComponent>
  </div>
</template>

<!------------------------------------------------------------------------------------------------->

<script setup lang="ts">
  import { EMIT } from "@/enum";
  import { FontIconType, FontIconSize, FontIconRotate, FontIconFlip, FontIconAnimate } from "@/icons/FontIconConstants";
  import FontIconComponent from '@/icons/FontIconComponent.vue';

  //////////////////////////////////////////////////////////////////////////////////////////////////
  //  PROPS
  interface I{{ upperCase class }}ButtonProps
  {
    fontIconSize?: string;
  }

  const props: Readonly<I{{ upperCase class }}ButtonProps> = withDefaults(defineProps<I{{ upperCase class }}ButtonProps>(), {
    fontIconSize: FontIconSize.LG
  });

  const _name: string = "{{ upperCase class }}Button";

  const emit = defineEmits([ EMIT.CLICK, ]);
</script>

<!------------------------------------------------------------------------------------------------->

<style scoped>

</style>
